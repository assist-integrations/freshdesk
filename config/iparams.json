{
    "domain": {
      "display_name": "Select the domain name that you have been using with Zoho Assist",
      "description": "For e.g., if your URL is \"assist.zoho.com\" \n then select your domain as US, \n if your URL is \"assist.zoho.eu\" then select your domain as EU, if your URL is \"assist.zoho.in\" then select your domain as IN.",
      "type": "radio",
      "options": [
        "US",
        "EU",
        "IN"
      ],
      "default_value": "US",
      "required": true
    }
}